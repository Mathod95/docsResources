apiVersion: ec2.aws.m.upbound.io/v1beta1
kind: RouteTable
metadata:
  name: private-rt
  namespace: crossplane-system
  labels:
    app: main-vpc
    type: private
spec:
  forProvider:
    region: eu-west-1
    vpcIdSelector:
      matchLabels:
        app: main-vpc
    tags:
      Name: private-rt
      ManagedBy: crossplane
  providerConfigRef:
    name: main
    kind: ProviderConfig